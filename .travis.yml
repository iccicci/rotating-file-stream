dist: focal
language: node_js
script: "yarn coverage"

node_js:
  - "24"
  - "22"
  - "20"

after_success:
  - "if [[ `node --version` =~ ^v24 ]] ; then curl https://qlty.sh | sh ; ~/.qlty/bin/qlty coverage publish coverage/lcov.info --override-branch $TRAVIS_BRANCH ; fi"
